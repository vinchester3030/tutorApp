export const getStudentColor = (name: string) => {
    if (name === 'Леня'){
        return '#ffc37f'
    } else if (name === 'Даня'){
        return 'green'
    } else {
        return '#6fe9a8'
    }
}